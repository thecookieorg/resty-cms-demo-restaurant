<nav class="navbar navbar-default" style="background: black;">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">RestyCMS</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% @profiles.each do |profile| %>
            <li><%= link_to 'Manage Locations', profiles_path %></li>
        <% end %>
        <% @socials.each do |social| %>
            <li><%= link_to 'Manage Social Media Links', edit_social_path(social) %></li>
        <% end %>
        <% @ordering_links.each do |ordering_link| %>
            <li><%= link_to 'Manage Online Ordeiring Link', edit_ordering_link_path(ordering_link) %></li>
        <% end %>
        <li><%= link_to('Logout', destroy_admin_session_path, :method => :delete) %></li>
        
        
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>